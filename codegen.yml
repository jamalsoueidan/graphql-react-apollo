overwrite: true
schema: "http://localhost:4000"
documents: "*.graphql"
generates:
  src/generated/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
